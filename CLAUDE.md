# 开发约束

- 上传 Marketplace 前须解包检查 VSIX 的 publisher 与目标发布者 ID 一致，不只检查源码清单。原因：旧安装包仍可能携带本地开发发布者，导致上传被拒。

- Marketplace 发布者资料使用作者公开身份与个人主页，插件介绍、图标和仓库链接放在扩展条目中。原因：发布者代表作者，可承载多个插件，不能与单个扩展混为一谈。

- 用户要求增强 Codex 自带功能时，优先复用原生界面、接口和状态，不另建平行面板。原因：独立历史选择框不符合在原生历史中管理会话的需求。
- 更换插件图标时同时核对扩展清单、编辑器和侧栏命令图标。原因：扩展图标不会自动覆盖工具栏单独配置的系统图标。
- 官方扩展更新后先验证资产兼容性，再应用可恢复补丁；未知资产不得强行写入。原因：内部接口不是稳定公开 API，不能以版本号相近推断兼容。
- 补丁实现变化后发布必须递增辅助扩展版本。原因：跨窗口防降级依靠备份中的版本号识别新版实现，不能重复发布同版本的不同补丁。
- 工具栏图标须在实际小尺寸下验证可见主体与相邻图标大小一致。原因：大画布留白会使工具栏中的图案偏小。
